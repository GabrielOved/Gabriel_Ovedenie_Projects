import EditAgenda from "./components/EditAgenda"; // Import main agenda component

function App() { // Root application component
  return (
    <div>
      {/* Render the Agenda feature */}
      <EditAgenda />
    </div>
  );
}

export default App; // Export App so it can be used as the entry point